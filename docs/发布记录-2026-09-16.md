# 2026-09-16 登录入口优化发布记录

## 版本与范围

- 发布标签：`release-2026-09-16`
- 应用 commit：`a639cdf2a8497eca2273eeec9270aeced1f3d88c`
- 合并来源：`feature/login-entry-refresh`
- 生产目录：`/opt/climbing-demo`
- 版本标记：`/opt/climbing-demo/DEPLOYED_COMMIT`
- 上一版回退目录：`/opt/climbing-demo.previous-54cea43-20260916`
- 数据库备份：`/opt/climbing-demo/deployment-backups/postgres-before-release-20260916T224755Z.sql.gz`

本次发布只调整登录入口的产品表达和视觉体验，不改变认证、权限、数据库结构或 Worker 算法。

## 页面变更

- 主标题改为“把每一次向上，都变成岩馆更好的下一步。”
- 说明改为“从岩点与线路建档，到摄像头识别和攀爬复盘，一处看清岩馆每天正在发生什么。”
- 删除 `MODULE 01` 和轻量 CRM 过渡文案。
- 三项能力改为“岩点资产管理、线路视觉建档、摄像头识别复盘”。
- 品牌统一为“Climbing 数字化运营平台”，同步更新页面标题和描述元数据。
- 桌面端保留左右分栏，增加攀爬路径视觉、核心能力卡片和聚焦式登录卡片；窄屏自动切换为上下布局。

## 发布验证

| 检查项 | 结果 |
| --- | --- |
| `DEPLOYED_COMMIT` | `a639cdf2a8497eca2273eeec9270aeced1f3d88c` |
| Azure 登录页 | 新标题、说明和三项核心能力已对外生效 |
| 桌面视觉 | 1280 × 720 实际页面检查通过，无横向溢出 |
| 生产首页 | HTTP 200 |
| API / Web / PostgreSQL | 健康 |
| Prisma migration | 35 个，无待执行迁移 |
| 数据库不变量 | 31 类检查全部为 0 |
| Vision Worker | 在线，2 条线路，心跳正常 |

本地发布前验证：`pnpm lint`、`pnpm typecheck`、`pnpm build` 全部通过；API 128 项、Web 37 项、Worker 29 项测试通过，8 项显式数据库集成测试按默认策略跳过。

## 发布与安全

- SSH 临时 `/32` 规则因端口不可达被立即删除，没有放宽为 Internet 来源。
- 发布包通过禁用公开访问的临时 Azure Blob 和短时只读 SAS 交付，再由 Azure VM Run Command 执行。
- 发布完成后删除远端与本地临时归档、临时脚本和整个临时 Storage 账户。
- NSG 最终只保留 HTTP、HTTPS 以及限定场馆 `/32` 的 GB28181 SIP/RTP 规则。

## 边界

- 本次没有修改登录鉴权逻辑；邮箱 Session、角色权限和组织隔离仍沿用原实现。
- 本次没有修改视觉 Worker；Azure Worker 在应用发布后持续在线。
- 动态公网 IP 自动白名单仍未合入 `main`。
